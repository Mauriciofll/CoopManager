# Driver PostgreSQL

Coloque aqui o arquivo `postgresql-*.jar` do driver JDBC do PostgreSQL.

Exemplo esperado:

```text
lib/postgresql-42.7.4.jar
```

O projeto compila sem esse arquivo, mas o modo `database.mode=postgres` precisa dele para abrir.
